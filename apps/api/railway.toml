[build]
builder = "NIXPACKS"

[deploy]
# Standard uvicorn call on $PORT
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"

[env]
ENVIRONMENT = "production"
