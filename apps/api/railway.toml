[build]
builder = "NIXPACKS"

[deploy]
# Standard start command, no extras
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"

[env]
ENVIRONMENT = "production"
